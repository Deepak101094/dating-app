import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Bio = () => {
	return (
		<View>
			<Text>index</Text>
		</View>
	);
};

export default Bio;

const styles = StyleSheet.create({});
